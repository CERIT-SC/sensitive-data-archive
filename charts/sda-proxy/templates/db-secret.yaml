apiVersion: v1
kind: Secret
metadata:
  name: sda-proxy-db-cnpg-superuser-secret
type: kubernetes.io/basic-auth
data:
  username: {{ .Values.credentials.db.user | b64enc }}
  password: {{ .Values.credentials.db.password | b64enc }}
