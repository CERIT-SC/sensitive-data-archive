{{- if .Values.tls.enable }}
apiVersion: v1
kind: Secret
metadata:
  name: sda-proxy-db-cert-secret
  labels:
    cnpg.io/reload: ""
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sda-proxy-db-cert
spec:
  secretName: sda-proxy-db-cert-secret
  usages:
    - server auth
  dnsNames:
    - sda-proxy-db-cnpg-rw
    - sda-proxy-db-cnpg-rw.{{ .Release.Namespace }}.svc.cluster.local
    - sda-proxy-db-cnpg-r
    - sda-proxy-db-cnpg-r.{{ .Release.Namespace }}.svc.cluster.local
    - sda-proxy-db-cnpg-ro
    - sda-proxy-db-cnpg-ro.{{ .Release.Namespace }}.svc.cluster.local
  issuerRef:
    name: {{ .Values.tls.issuer }}
    kind: Issuer
    group: cert-manager.io
{{- end }}
